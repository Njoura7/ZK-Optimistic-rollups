% =========================================
% CHAPTER 4: FRAMEWORK DESIGN
% =========================================
\chapter{Framework Design and Architecture}
\label{ch:framework}

This chapter describes the design of the testing framework. The goal is straightforward: run both rollup types against the same Layer~1 node and measure what actually happens. The framework needs to let us measure finality latency, recovery time after crashes, and resource costs. It also needs to be reproducible so other researchers can verify the results.

\section{Design Requirements}

Based on the gaps in the literature, we identified five requirements that drove the design:

First, we need to measure finality precisely. Both rollups must record when transactions are submitted and when they become irreversible. This is Gap 1 from Chapter~\ref{ch:relatedwork}—we want actual numbers, not estimates.

Second, we need to see what happens when things break. Specifically, we simulate sequencer crashes and measure how long recovery takes and whether data is lost. This addresses Gap 2.

Third, both rollups must run against the same Layer~1 node with identical conditions. This is critical because if they run against different nodes or at different times, we cannot know whether differences come from the architecture or from the environment. This is Gap 4.

Fourth, the entire testbed must be open-source and deployable with a single Docker command. This is Gap 5. If other researchers cannot easily reproduce the setup, the results are not useful.

Fifth, we need to export enough metrics so we can actually analyze what happened. Throughput, latency, batch sizes, proof generation time—all of these need to be recorded.

Three additional requirements follow from the design but are not fully implemented: extensibility (adding new rollup types should be straightforward), accessibility (it should run on standard developer hardware), and censorship resistance (partially addressed through test design).

\section{System Architecture}

The framework has four components that work together to compare both rollup types.

The first component is a local Ethereum node running Hardhat~\cite{hardhat2024}. This node hosts both the ZKVerifier and OptimisticVerifier contracts. Because both contracts run on the same blockchain instance, any network delay or block timing variation affects both rollups equally. This prevents us from accidentally favoring one architecture.

The second component is two sequencers, each running as a separate Python process. They run within the same Docker~\cite{docker2024} container and connect to the shared Hardhat node. The ZK sequencer listens on port 5000; the Optimistic sequencer listens on port 5001. Running them as separate processes means if one crashes, the other continues—we can measure failures in isolation.

The third component is a web dashboard built with Flask~\cite{flask2024}. It shows real-time metrics from both sequencers and lets us trigger test scenarios. When we want to simulate a sequencer crash or inject a Layer~1 delay, we do it from the dashboard.

The fourth component is the metrics stack. Prometheus~\cite{prometheus2024} scrapes metrics from each sequencer (ports 9100 and 9101) and Grafana~\cite{grafana2024} visualizes the data. This gives us time-series records we can analyze later.

Figure~\ref{fig:architecture} shows how these pieces connect.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.85\textwidth]{architecture/framework-architecture.png}
    \caption{System architecture: components, ports, and data flow.
    \textbf{Placeholder location:} \texttt{ths/images/architecture/framework-architecture.png}.
    This diagram should show the four main components (Hardhat L1, ZK Sequencer, Optimistic Sequencer,
    Metrics Stack) and their port bindings.}
    \label{fig:architecture}
\end{figure}

If the image is not yet available, the ASCII diagram above is still present in the source code and can be used as a temporary reference.

To deploy the framework, we run a single Docker Compose~\cite{compose2024} command. This starts all components at once. Table~\ref{tab:eval-dimensions} lists what we measure and how.

\begin{table}[h]
    \centering
    \begin{tabular}{l|l|l}
        \hline
        \textbf{What We Measure} & \textbf{Method} & \textbf{Unit} \\
        \hline
        Finality time & Record submission block and finality block & Blocks (or seconds) \\
        \hline
        Throughput & Count transactions per time window & Transactions/second \\
        \hline
        Batching & Record batch size and timing & Transactions per batch \\
        \hline
        Proof cost & Time to generate, gas to verify & Seconds and wei \\
        \hline
        State validity & Check if state roots match & Binary \\
        \hline
    \end{tabular}
    \caption{Measurement dimensions and methods}
    \label{tab:eval-dimensions}
\end{table}

\section{How Transactions Flow Through the Framework}

When we submit a transaction, here is what happens. The test scenario sends HTTP POST to one of the sequencers. The sequencer adds the transaction to a pending pool. When the pool reaches a threshold (100 transactions for ZK, 200 for Optimistic), the sequencer creates a batch. The sequencer computes the state root. For ZK, it also generates a proof. Then it sends the commitment to Layer~1.

The Layer~1 contract either verifies the proof (ZK) or opens a challenge window (Optimistic). For ZK, finality happens when the proof is verified and five additional blocks have passed. For Optimistic, finality happens when the challenge window closes and no dispute was filed.

We record timestamps at each step. The difference between submission block and finality block is the latency we measure. This latency includes both on-chain delays and off-chain computation.

\section{Why These Design Choices}

We chose to use SHA-256 for proof generation instead of real zk-SNARKs. Real proofs require expensive elliptic-curve computations and specialized hardware. SHA-256 is fast enough that we can run many test scenarios quickly. The key point is that both sequencers still generate something and submit it—the architectural difference remains even though we simplified the proof. Important caveat: The 1.9\% proof overhead we measure in Chapter~\ref{ch:evaluation} is a lower bound. Production ZK~Rollups report overhead ranging from seconds to minutes per batch, depending on hardware and circuit complexity.

We set the ZK batch size to 100 transactions and Optimistic to 200. Why? Because larger batches amortize the cost of Layer~1 submission better, but only if proof generation is not a bottleneck. With ZK, proof generation limits how fast the sequencer can work, so smaller batches keep finality latency low. With Optimistic, there is no proof overhead, so larger batches are fine. These numbers reflect the real trade-offs.

We compress the Optimistic challenge window to 10 blocks instead of 7 days. Production systems cannot wait 7 days for every test. But 10 blocks (roughly 2 minutes on Hardhat) preserves the structure of the security model. Any Layer~1 reorganization that is short enough to be practical to test will not exceed 5 blocks anyway.

We run both sequencers against the same Layer~1 node. Some frameworks test each system in isolation. That introduces noise. Network delays, block timings, gas prices—these vary over time. By running both simultaneously against identical conditions, we eliminate these variables. Any difference we observe comes from the architecture, not the environment.

\section{Metrics We Export}

Each sequencer exports metrics to Prometheus on a dedicated port. The metrics are simple: transaction count, batch count, throughput, finality latency, and proof generation time (ZK only). We store these as histograms so we can compute percentiles—not just the average, but the P95 and P99 latencies, which matter more for user experience.

Table~\ref{tab:research-questions} shows how each research question maps to what we measure.

\begin{table}[h]
    \centering
    \begin{tabular}{l|l|p{5cm}}
        \hline
        \textbf{Question} & \textbf{What We Measure} & \textbf{Test File} \\
        \hline
        RQ1: Finality & Time from submission to finality & \texttt{tests/integration/test\_dual\_sequencer.py} \\
        \hline
        RQ2: Recovery & Time and cost to recover after crash & \texttt{tests/adversarial/test\_sequencer\_crash.py} \\
        \hline
        RQ3: Security cost & Proof generation time and gas overhead & \texttt{tests/integration/test\_dual\_sequencer.py} \\
        \hline
    \end{tabular}
    \caption{Research questions and measurements}
    \label{tab:research-questions}
\end{table}

\subsection{Technology Stack}

Table~\ref{tab:tech-stack} lists the technologies used for each component along with the rationale for their selection. The primary criteria were maturity, ease of integration, and suitability for rapid prototyping within an academic context. Every tool in the stack is open-source and well-documented, which supports the reproducibility requirement established in Section~\ref{ch:framework}.


\section{Configuration}

The framework parameters are configurable via environment variables. This allows researchers to test how different architectural choices affect performance.

\begin{table}[h]
    \centering
    \begin{tabular}{l|l|l}
        \hline
        \textbf{Parameter} & \textbf{Default} & \textbf{Purpose} \\
        \hline
        ZK_BATCH_SIZE & 100 & Transactions per ZK batch \\
        \hline
        ZK_BATCH_INTERVAL & 5 & Seconds between ZK batch checks \\
        \hline
        ZK_GAS_LIMIT & 200000 & Gas allocated to ZK submissions \\
        \hline
        OPT_BATCH_SIZE & 200 & Transactions per Optimistic batch \\
        \hline
        OPT_BATCH_INTERVAL & 8 & Seconds between Optimistic batch checks \\
        \hline
        OPT_GAS_LIMIT & 150000 & Gas allocated to Optimistic submissions \\
        \hline
        L1_RPC & http://localhost:8545 & Layer 1 RPC endpoint \\
        \hline
    \end{tabular}
    \caption{Configurable framework parameters}
    \label{tab:config-params}
\end{table}

These parameters can be set via Docker environment variables when starting the framework, allowing experimental variation without code changes. For example:

\begin{verbatim}
docker-compose up -d
# Now modify one parameter and re-run the full stack:
ZK_BATCH_SIZE=150 OPT_BATCH_INTERVAL=5 docker-compose up -d
\end{verbatim}

This approach lets researchers measure how batch size, polling frequency, and gas allocation affect finality latency, throughput, and cost. Changing any parameter and re-running the test suite provides immediate feedback on its impact.

We chose Hardhat for Layer~1 because it is fast and easy to configure. We chose Python and Flask for the sequencers because we wanted to iterate quickly, not optimize for production performance. Prometheus and Grafana are standards in the monitoring space. Docker Compose handles deployment. None of these choices are controversial—they are reliable tools that let us focus on measuring rollup behavior, not on building production infrastructure.

\section{How the System Actually Works}

When a test submits a transaction, the sequencer receives it via HTTP and adds it to a queue. The sequencer has a background thread that wakes up every few seconds and checks if there are enough transactions to form a batch. If yes, it creates a commitment, sends it to Layer~1, and records how long that took.

For ZK, "creating a commitment" means computing two hash values and packaging them together. For Optimistic, it means computing one hash.

Layer~1 then does something different depending on the type. For ZK, it checks that a proof was provided (we do not verify the proof itself to keep things fast). For Optimistic, it records that a challenge window has started and waits to see if anyone disputes it.

A commitment reaches finality when either the proof is verified and five blocks have passed (ZK), or the challenge window has closed and no dispute was filed (Optimistic).

We record every timestamp—submission, inclusion in a block, and finality. The difference between submission and finality is the latency we measure in Chapter~\ref{ch:evaluation}.

One note on failure handling: If a sequencer crashes during batch assembly, pending transactions are lost. This is a limitation of the test framework (production systems use persistent mempools and recovery mechanisms). However, because the two sequencers are independent processes, a crash in one does not affect the other—we can isolate and measure failures separately.

The batch creation loop is simple: wait for the pool to fill, create a batch, submit it, then sleep and repeat. The ZK sequencer sleeps for 5 seconds and uses a batch of 100 transactions. The Optimistic sequencer sleeps for 8 seconds and uses 200 transactions. These numbers reflect the different trade-offs between proof overhead and Layer~1 gas cost.

\section{What This Chapter Establishes}

The framework gives us a controlled environment in which both rollup types run against identical conditions: the same Layer~1 node, the same hardware, the same network. The design choices documented here---batch sizes, challenge window compression, SHA-256 as a proof stand-in---are deliberate compromises between experimental practicality and architectural faithfulness. Each one is flagged where it appears so that results can be interpreted with appropriate scope.

Two intentional omissions are worth restating before moving to the code. First, transactions are not executed against a real state tree; state roots are derived by hashing batch data, not by applying each transaction to a storage trie. Second, ZK proofs are not verified cryptographically; the contract checks that a proof is present, not that it is correct. Both simplifications preserve the structural properties that matter for the comparison---ZK requires a proof at submission time, Optimistic requires a challenge window before finality---without the hardware and time cost that real cryptographic operations would impose.

Chapter~\ref{ch:implementation} describes how each component was built in code: what the contracts store, how the sequencers decide when to batch, and how the metrics layer records timestamps at each step.
