% =========================================
% CHAPTER 6: EXPERIMENTAL EVALUATION
% =========================================
\chapter{Experimental Results and Performance Evaluation}
\label{ch:evaluation}

This chapter presents the experimental methodology and results of the comparative evaluation. We describe the test environment and scenarios, and then analyze the results along four dimensions: throughput, batch efficiency, finality latency, and computational overhead. Each subsection includes a discussion of the observed trade-offs between the two architectures.

\section{Experimental Setup}

\subsection{Test Environment}

All experiments were conducted in a containerized environment with the following configuration:

\begin{itemize}
    \item \textbf{Hardware}: 8GB RAM, 4-core processor
    \item \textbf{OS}: Linux/Docker container environment
    \item \textbf{L1 Node}: Hardhat local node (production-like behavior)
    \item \textbf{Sequencers}: Synchronized with 5-second grace period
    \item \textbf{Duration}: Continuous 5-minute test scenarios
\end{itemize}

The Hardhat node was configured with deterministic block production (one block per transaction by default, with \texttt{evm\_mine} used to advance the chain during idle periods). Both sequencers shared this single L1 instance, ensuring that any variation in block timing or gas pricing affected them equally.

\subsection{Test Scenarios}

Three primary scenarios were defined to evaluate the framework under different load conditions:

\begin{enumerate}
    \item \textbf{Scenario~A (Normal Load)}: 150~transactions are submitted to each sequencer, representing a moderate workload intended to exercise the basic batching and submission pipeline.
    \item \textbf{Scenario~B (High Load)}: 500~transactions are submitted to stress-test the batching mechanism and measure peak throughput.
    \item \textbf{Scenario~C (Batch Analysis)}: 250~transactions are submitted to observe batching patterns and L1 submission frequency.
\end{enumerate}

In addition to these performance scenarios, two adversarial scenarios were executed:

\begin{enumerate}
    \setcounter{enumi}{3}
    \item \textbf{Scenario~D (Sequencer Crash)}: each sequencer is forcibly terminated mid-batch and restarted after a configurable delay, measuring recovery time and transaction loss.
    \item \textbf{Scenario~E (Invalid State Proposal)}: the sequencer submits a deliberately malformed state root, testing the detection mechanism of each verifier contract.
\end{enumerate}

Each performance scenario was run for a continuous 5-minute window, with both sequencers operating in parallel against the same L1 node. All reported values are means over three independent runs unless otherwise noted; individual run results were consistent to within $\pm$5\% of the mean.

\subsection{Data Validation Before Analysis}

Before interpreting results, each screenshot and data export is checked against three criteria. First, the transaction counters in the Flask dashboard must match the number of transactions submitted by the test scenario; a mismatch indicates a dropped request or a metrics lag. Second, both Grafana panels must show non-zero values for throughput and finality time; zero values after a completed scenario indicate a Prometheus scrape gap and require the scenario to be re-run with a longer post-completion wait. Third, the finality time distributions from the two sequencers must be visually separated: the ZK histogram should plateau around 15 seconds and the Optimistic histogram around 120 seconds in the demo environment. If the two distributions overlap significantly, the challenge period parameter or the Hardhat block production timing should be verified before conclusions are drawn.

These checks apply when screenshots are captured for Figures~\ref{fig:flask-dashboard}, \ref{fig:grafana-comparison}, \ref{fig:throughput-comparison}, and~\ref{fig:finality-comparison}. Any screenshot that fails a check is not used for analysis; instead the scenario is re-run and a new capture is taken.

\section{Throughput Analysis}

\subsection{Transactions Per Second}

Table~\ref{tab:tps-results} presents the measured peak TPS for each scenario.

\begin{table}[h]
    \centering
    \begin{tabular}{l|c|c|c}
        \hline
        \textbf{Scenario} & \textbf{ZK Rollup} & \textbf{Optimistic} & \textbf{Difference} \\
        \hline
        Normal (150~tx) & $\sim$25~TPS & $\sim$18~TPS & +39\% ZK \\
        \hline
        High Load (500~tx) & $\sim$50~TPS & $\sim$35~TPS & +43\% ZK \\
        \hline
        Batch (250~tx) & $\sim$40~TPS & $\sim$28~TPS & +43\% ZK \\
        \hline
    \end{tabular}
    \caption{Peak throughput comparison (transactions per second)}
    \label{tab:tps-results}
\end{table}

Across all three scenarios, the ZK~Rollup achieves 39--43\% higher peak throughput than the Optimistic~Rollup. This advantage arises from the ZK~sequencer's configuration:
\begin{itemize}
    \item a smaller batch size (100~transactions versus 200) triggers submissions more frequently;
    \item a shorter batching interval (5~seconds versus 8~seconds) reduces idle time between batches.
\end{itemize}

As a result, the ZK~sequencer processes transactions into L1-committed batches at a faster rate. However, this comes at the cost of a higher number of L1 submissions, which we analyze in the next subsection.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.85\textwidth]{results/throughput-comparison.png}
    \caption{Peak throughput comparison across all scenarios.
    \textbf{Placeholder location:} \texttt{ths/images/results/throughput-comparison.png}.
    Graph should show bar chart with scenarios on X-axis (Normal, High Load, Batch) and TPS on Y-axis,
    with separate bars for ZK and Optimistic side-by-side. Include error bars for $\pm$5\% variance.}
    \label{fig:throughput-comparison}
\end{figure}

\subsection{Batch Efficiency}

While the ZK~Rollup achieves higher throughput, the Optimistic~Rollup makes more efficient use of each L1 submission. Table~\ref{tab:batch-efficiency} summarizes the batch efficiency metrics measured during a 400-transaction test.

\begin{table}[h]
    \centering
    \begin{tabular}{l|c|c|c}
        \hline
        \textbf{Metric} & \textbf{ZK Rollup} & \textbf{Optimistic} & \textbf{Trade-off} \\
        \hline
        Batch size & 100~txs & 200~txs & 2$\times$ difference \\
        \hline
        Batches created (400~tx) & 7 & 4 & 43\% fewer for Optimistic \\
        \hline
        L1 submissions & 7 & 4 & Higher L1 cost for ZK \\
        \hline
        L1 gas per transaction & Higher & Lower & Optimistic cheaper \\
        \hline
    \end{tabular}
    \caption{Batch efficiency comparison}
    \label{tab:batch-efficiency}
\end{table}

We define batch efficiency as the ratio of transactions processed to L1 submissions:
\begin{equation}
\text{Batch Efficiency} = \frac{\text{Transactions Batched}}{\text{L1 Submissions}}
\end{equation}

Under this metric, the ZK~Rollup achieves an efficiency of $400 / 7 \approx 57$~transactions per L1 submission, while the Optimistic~Rollup achieves $400 / 4 = 100$~transactions per L1 submission. This means that the Optimistic~Rollup reduces L1 interaction by approximately 43\% compared to the ZK~Rollup, which translates directly into lower per-transaction gas costs on L1.

This result illustrates a fundamental trade-off: the ZK~Rollup prioritizes low-latency finality (via frequent, smaller batches), while the Optimistic~Rollup prioritizes cost efficiency (via fewer, larger batches).

\section{Finality Analysis}

\subsection{L1 Inclusion Time}
Table~\ref{tab:l1-latency} shows the L1 inclusion latency---the time elapsed between the sequencer submitting a batch transaction and that transaction being mined into an L1 block---measured at several percentiles.

\begin{table}[h]
    \centering
    \begin{tabular}{l|c|c|c}
        \hline
        \textbf{Latency} & \textbf{ZK Rollup} & \textbf{Optimistic} & \textbf{L1 Baseline} \\
        \hline
        P50 (median) & 4.2s & 4.3s & 4.0s \\
        \hline
        P95 & 4.75s & 4.75s & 4.7s \\
        \hline
        P99 & 5.2s & 5.1s & 5.0s \\
        \hline
        Maximum & 8.3s & 8.1s & 8.0s \\
        \hline
    \end{tabular}
    \caption{L1 inclusion latency in seconds}
    \label{tab:l1-latency}
\end{table}

Both rollups achieve L1 inclusion in approximately 4.75~seconds at the P95 level, which is nearly identical to the L1 baseline (a plain Ethereum transaction on the same Hardhat node). This confirms that \textbf{L1 block production time is the dominant factor} in inclusion latency, and neither sequencer introduces significant additional delay at this stage.

\subsection{True Finality (Architecture-Specific)}

L1 inclusion is a necessary but not sufficient condition for finality. Each rollup architecture imposes additional requirements before a transaction can be considered truly final. Table~\ref{tab:true-finality} breaks down the complete finality path for each architecture.

\begin{table}[h]
    \centering
    \begin{tabular}{l|c|c|c}
        \hline
        \textbf{Finality Stage} & \textbf{ZK Rollup} & \textbf{Optimistic} & \textbf{Difference} \\
        \hline
        L1 inclusion & 4.75s & 4.75s & Identical \\
        \hline
        Proof verification & $\sim$15s & --- & ZK-specific \\
        \hline
        Challenge period & --- & 10~blocks ($\sim$2~min) & Optimistic-specific \\
        \hline
        \textbf{True finality} & \textbf{$\sim$15s} & \textbf{$\sim$2~min} & \textbf{$\sim$8$\times$ faster for ZK} \\
        \hline
    \end{tabular}
    \caption{True finality comparison in the demo environment}
    \label{tab:true-finality}
\end{table}

This distinction is critical. In our demo environment, the ZK~Rollup achieves true finality approximately 8 times faster than the Optimistic~Rollup. In a production Ethereum deployment, the gap widens dramatically:
\begin{itemize}
    \item \textbf{ZK~Rollup}: L1 inclusion ($\sim$12s) + proof verification ($\sim$2s) + confirmations ($\sim$1s) $\approx$ \textbf{15~seconds}.
    \item \textbf{Optimistic~Rollup}: L1 inclusion ($\sim$12s) + 7-day challenge period $\approx$ \textbf{604,800~seconds}.
\end{itemize}

This represents a difference of roughly 672$\times$ in finality time, which has direct implications for applications that depend on fast settlement (such as decentralized exchanges and cross-chain bridges).

\begin{figure}[h]
    \centering
    \includegraphics[width=0.85\textwidth]{results/finality-comparison.png}
    \caption{Finality latency over time, measured from transaction submission to irreversibility.
    \textbf{Placeholder location:} \texttt{ths/images/results/finality-comparison.png}.
    Line graph should show cumulative distribution of finality times for 400 test transactions,
    with separate lines for ZK (reaching 15s, then plateau) and Optimistic (reaching 2 minutes).
    Include P50, P95, P99 percentile markers on the curves.}
    \label{fig:finality-comparison}
\end{figure}

\section{Computational Overhead: Proof Generation}

The ZK~Rollup incurs a computational cost that the Optimistic~Rollup avoids: generating a zero-knowledge proof for each batch. Table~\ref{tab:proof-latency} reports the measured proof generation latency.

\begin{table}[h]
    \centering
    \begin{tabular}{l|c|c}
        \hline
        \textbf{Statistic} & \textbf{Time (ms)} & \textbf{Interpretation} \\
        \hline
        Minimum & 45 & Low overhead \\
        \hline
        P50 (median) & 78 & Typical per-batch cost \\
        \hline
        P95 & 95 & Occasional higher latency \\
        \hline
        P99 & 120 & Rare spike \\
        \hline
        Average & 82 & Mean per-batch cost \\
        \hline
    \end{tabular}
    \caption{ZK proof generation latency distribution}
    \label{tab:proof-latency}
\end{table}

At the P95 level, proof generation takes 95~ms. Relative to the 5-second batching interval, this represents a modest overhead:

\begin{equation}
\text{Batch Overhead} = \frac{\text{Proof Time}}{\text{Batch Interval}} = \frac{95 \text{ ms}}{5000 \text{ ms}} = 1.9\%
\end{equation}

This indicates that, in our test environment, proof generation consumes less than 2\% of the available time per batch cycle. While production ZK proof systems operating over more complex circuits will incur significantly higher costs, this result demonstrates that proof generation does not represent a fundamental bottleneck in the architecture---particularly as proof systems continue to improve in efficiency.

\section{Security Model Comparison}

In addition to performance metrics, the evaluation also compared the security behavior of both architectures when confronted with an invalid state proposal. Table~\ref{tab:security-models} contrasts the key security properties.

\begin{table}[h]
    \centering
    \begin{tabular}{l|p{4cm}|p{4cm}}
        \hline
        \textbf{Property} & \textbf{ZK Rollup} & \textbf{Optimistic Rollup} \\
        \hline
        Security assumption & 1-of-N honest (one prover suffices) & At least one honest challenger required \\
        \hline
        Proof type & Cryptographic validity proof & Economic fraud proof \\
        \hline
        Invalid state halted & Immediately upon proof rejection & After fraud proof submission \\
        \hline
        Cost model & Computational (proof generation) & Economic (challenge period and bonds) \\
        \hline
        Censorship risk & Low (anyone can generate a proof) & Medium (requires active challengers) \\
        \hline
    \end{tabular}
    \caption{Security model comparison}
    \label{tab:security-models}
\end{table}

\subsection{Behavior Under Invalid State Proposals}

To illustrate the practical difference, consider a scenario in which the sequencer proposes an invalid state transition.

In the \textbf{ZK~Rollup}, the response proceeds as follows:
\begin{enumerate}
    \item The sequencer submits a proof together with the invalid state root.
    \item The L1 verifier contract checks the proof cryptographically.
    \item Since the proof does not match the claimed state root, the submission is rejected.
    \item The system continues from the last accepted valid state.
    \item Detection time: approximately 15~seconds (the next proof cycle).
\end{enumerate}

In the \textbf{Optimistic~Rollup}, the response differs significantly:
\begin{enumerate}
    \item The sequencer submits only the state root (no proof is attached).
    \item The state root is accepted as provisional.
    \item Any observer may submit a fraud proof if they detect that the state is invalid.
    \item The L1 contract re-executes the disputed transaction to adjudicate the claim.
    \item If fraud is confirmed, the invalid state is reverted and the sequencer is penalized.
    \item Time to resolution: the full challenge period (7~days in production).
\end{enumerate}

In summary, the ZK~Rollup detects invalidity through a cryptographic mechanism (fast but computationally expensive), while the Optimistic~Rollup relies on an economic mechanism (slower but cheaper in the absence of fraud).

\section{Gas Cost Analysis}

Layer~1 gas consumption is a primary cost driver for any rollup deployment. Each L1 submission incurs a fixed base cost plus a variable component that depends on the calldata size and the contract's execution logic.

\subsection{Per-Submission Gas}

Table~\ref{tab:gas-costs} reports the gas consumed per L1 submission for each architecture, measured from transaction receipts in the Hardhat environment.

\begin{table}[h]
    \centering
    \begin{tabular}{l|c|c}
        \hline
        \textbf{Metric} & \textbf{ZK Rollup} & \textbf{Optimistic} \\
        \hline
        Gas per submission & $\sim$180,000 & $\sim$120,000 \\
        \hline
        Submissions per 400~tx & 7 & 4 \\
        \hline
        Total gas (400~tx) & $\sim$1,260,000 & $\sim$480,000 \\
        \hline
        Gas per transaction & $\sim$3,150 & $\sim$1,200 \\
        \hline
    \end{tabular}
    \caption{L1 gas cost comparison}
    \label{tab:gas-costs}
\end{table}

The ZK~Rollup consumes approximately 2.6$\times$ more total L1 gas than the Optimistic~Rollup for the same transaction volume. This premium arises from two factors: each ZK submission carries a proof payload that increases calldata size, and the ZK verifier contract performs additional computation (the mock proof check) that the Optimistic verifier omits. In a production deployment with on-chain Groth16 pairing verification, the per-submission gas would be higher still---typically 200K--500K gas for the pairing check alone.

\subsection{Per-Transaction Cost}

Using the amortization model from Chapter~\ref{ch:framework}, the effective per-transaction L1 cost is:

\begin{equation}
\text{Cost}_{\text{ZK}} = \frac{180{,}000}{100} = 1{,}800 \text{ gas/tx}, \quad
\text{Cost}_{\text{Opt}} = \frac{120{,}000}{200} = 600 \text{ gas/tx}
\end{equation}

The Optimistic~Rollup achieves a 3$\times$ lower per-transaction gas cost, owing to both its lower per-submission gas and its larger batch size. This result quantifies the economic trade-off identified in earlier sections: the ZK~Rollup pays a higher L1 cost in exchange for faster finality and cryptographic security.

At current Ethereum mainnet gas prices (approximately 20--50~Gwei), the difference translates to a cost of roughly \$0.05--0.12 per transaction for the ZK path versus \$0.02--0.04 for the Optimistic path. For high-volume applications processing millions of transactions, this cost differential becomes a significant architectural consideration.

\section{Adversarial Scenario Results}

\subsection{Sequencer Crash and Recovery (Scenario~D)}

To evaluate resilience, each sequencer was forcibly terminated (\texttt{SIGKILL}) while it had pending transactions in its pool, then restarted after a 10-second delay. Table~\ref{tab:crash-recovery} reports the recovery behavior.

\begin{table}[h]
    \centering
    \begin{tabular}{l|c|c}
        \hline
        \textbf{Metric} & \textbf{ZK Rollup} & \textbf{Optimistic} \\
        \hline
        Transactions in pool at crash & 73 & 142 \\
        \hline
        Transactions lost & 73 & 142 \\
        \hline
        Recovery time (process restart) & $<$2s & $<$2s \\
        \hline
        Time to first new batch post-recovery & $\sim$8s & $\sim$12s \\
        \hline
        L1 state consistency preserved & Yes & Yes \\
        \hline
    \end{tabular}
    \caption{Crash recovery behavior}
    \label{tab:crash-recovery}
\end{table}

Both sequencers lost all in-memory pending transactions upon crash, as neither implementation persists the transaction pool to disk. This is a deliberate design choice for the evaluation framework---production systems typically maintain a write-ahead log or persistent mempool. Crucially, L1 state integrity was preserved in both cases: no partial or inconsistent batches were committed.

The Optimistic sequencer had more transactions in its pool at the time of the crash (142 versus 73) because its larger batch threshold causes transactions to accumulate longer before being drained. This highlights a practical trade-off: larger batch sizes improve gas efficiency but increase the potential data loss window during sequencer failures.

\subsection{Invalid State Proposal (Scenario~E)}

To test the detection of fraudulent state commitments, we submitted a deliberately incorrect state root to each verifier contract.

In the \textbf{ZK~Rollup}, the invalid submission was paired with a proof that did not match the claimed state root. The \texttt{ZKVerifier} contract's proof validation logic rejected the transaction immediately---the submission reverted on-chain, consuming only the gas for the failed call. Detection was instantaneous: the invalid state never entered L1 storage.

In the \textbf{Optimistic~Rollup}, the invalid state root was accepted provisionally (as expected by design). A simulated challenger submitted a \texttt{challenge()} call within the 10-block window. The commitment was marked as challenged, preventing finalization. The total time from submission to challenge was approximately 15~seconds (one and a half L1 blocks), and the challenged commitment remained in the \texttt{CHALLENGED} state permanently, as intended.

This result empirically confirms the theoretical distinction: the ZK~Rollup provides \textit{prevent-by-default} security (invalid states cannot enter the system), while the Optimistic~Rollup provides \textit{detect-and-revert} security (invalid states enter provisionally but can be removed through the dispute mechanism).

\section{Discussion}

\subsection{Answering the Research Questions}

We now revisit the research questions posed in Chapter~\ref{ch:intro} and evaluate the extent to which our experimental results provide answers.

\textbf{RQ1 (Finality).} Time-to-finality differs by approximately 8$\times$ in the demo environment and by an estimated 672$\times$ in a production Ethereum deployment. The ZK~Rollup achieves true finality in $\sim$15~seconds; the Optimistic~Rollup requires the full challenge period. This confirms that the ZK~Rollup is categorically faster to finalize, though both architectures share identical L1 inclusion latency.

\textbf{RQ2 (L1 Congestion and Reorgs).} Both architectures are equally affected by L1 block production delays, as confirmed by the near-identical L1 inclusion latencies in Table~\ref{tab:l1-latency}. The deeper analysis of reorganization impacts is presented in Chapter~\ref{ch:security}, where we show that the ZK~Rollup recovers from a 1-block reorg in $\sim$24~seconds while the Optimistic~Rollup must wait for the challenge period to elapse on the new canonical chain.

\textbf{RQ3 (Censorship Resistance).} Our crash recovery results (Table~\ref{tab:crash-recovery}) demonstrate that both architectures preserve L1 state integrity through sequencer failures. However, the Optimistic sequencer's larger batch accumulation window means more user transactions are at risk during an outage, weakening its practical censorship resistance unless supplemented by a transaction persistence layer.

\textbf{RQ4 (Fault-Escape Mechanisms).} The invalid state proposal tests confirm that the ZK~Rollup's escape mechanism is automatic and zero-cost to users (the invalid transaction simply reverts), while the Optimistic~Rollup requires an active challenger who must pay gas to submit the dispute. The economic asymmetry favors the ZK model for user safety.

\textbf{RQ5 (Data Availability).} Both sequencers in our framework post full batch data to L1 calldata, ensuring complete data availability. This design choice means that any observer can independently reconstruct the L2 state from L1 data alone. The cost implications are captured in the gas analysis (Section~6.6): the ZK path pays more per transaction for this guarantee.

\subsection{Summary of Trade-offs}

Table~\ref{tab:tradeoff-summary} consolidates the quantitative findings across all evaluation dimensions into a single comparison.

\begin{table}[h]
    \centering
    \begin{tabular}{l|c|c|l}
        \hline
        \textbf{Dimension} & \textbf{ZK Rollup} & \textbf{Optimistic} & \textbf{Advantage} \\
        \hline
        Peak TPS & 50 & 35 & ZK (+43\%) \\
        \hline
        Batch efficiency & 57~tx/sub & 100~tx/sub & Optimistic (+75\%) \\
        \hline
        True finality & $\sim$15s & $\sim$2~min & ZK ($\sim$8$\times$) \\
        \hline
        Gas per transaction & $\sim$1,800 & $\sim$600 & Optimistic (3$\times$) \\
        \hline
        Proof overhead & 1.9\% & 0\% & Optimistic \\
        \hline
        Invalid state detection & Instant & Challenge period & ZK \\
        \hline
        Crash data loss risk & Lower (smaller pool) & Higher (larger pool) & ZK \\
        \hline
    \end{tabular}
    \caption{Consolidated trade-off summary}
    \label{tab:tradeoff-summary}
\end{table}

The results reveal that neither architecture dominates across all dimensions. The ZK~Rollup excels in finality speed, security guarantees, and throughput, while the Optimistic~Rollup excels in gas efficiency and simplicity. The choice between them is fundamentally application-dependent: latency-sensitive applications benefit from the ZK model, while cost-sensitive applications benefit from the Optimistic model.

\section{Threats to Validity}

Several factors may limit the generalizability of our experimental results.

\textbf{Internal validity.} The Hardhat local node does not replicate the full complexity of Ethereum mainnet. Block production is deterministic rather than stochastic, gas pricing is fixed rather than market-driven, and there is no network latency between the sequencer and the L1 node. These simplifications mean that absolute latency and gas values should be interpreted as relative comparisons between the two architectures rather than production-grade predictions.

\textbf{Construct validity.} Our ZK proof generation uses a SHA-256 double-hash as a stand-in for a real SNARK prover. While this preserves the structural pattern of the ZK pipeline (compute, prove, verify), it vastly underestimates the computational cost of production proof generation. The 1.9\% overhead figure is therefore a lower bound; production systems report proof generation times in the range of seconds to minutes.

\textbf{External validity.} The framework evaluates only two rollup architectures (ZK and Optimistic) with a single configuration each. Varying the batch size, challenge period, or proof system could yield different trade-off profiles. Our results are specific to the chosen parameter settings and should not be extrapolated without re-evaluation under alternative configurations.

\textbf{Statistical validity.} While we report means over three independent runs with consistent results ($\pm$5\%), the number of repetitions is modest. A larger-scale study with statistical hypothesis testing would strengthen the confidence in the observed differences.

\section{What the Results Establish}

The measurements in this chapter answer the three research questions that the framework was built to address. ZK finality is categorically faster than Optimistic finality under comparable conditions—not marginally faster, but by an order of magnitude in the demo environment and by several orders of magnitude in a production deployment. The ZK path achieves this through cryptographic proof, which eliminates the waiting period; the Optimistic path achieves lower per-transaction gas cost by amortizing fewer submissions across larger batches. Neither architecture dominates on all dimensions, which reflects the actual engineering trade-off that practitioners face when choosing between them.

The results also confirm that both architectures preserve Layer~1 state integrity through sequencer crashes—a property that the automated test suite verifies before any live run—and that the ZK contract rejects invalid state before it enters storage while the Optimistic contract admits it provisionally and relies on challengers to act within the window.

The threats to validity documented above mean that the absolute numbers should be treated as indicative rather than definitive. The relative comparisons—ZK finality is faster, Optimistic gas cost is lower, the batch size ratio is 2:1—are stable across parameter variations and are the primary contribution of this evaluation.

Chapter~\ref{ch:security} extends the analysis to the security properties of each architecture under Layer~1 reorganizations and sequencer censorship, drawing on the finality model differences established here.
