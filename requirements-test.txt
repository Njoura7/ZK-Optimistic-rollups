# Minimal dependencies for running the test suite locally.
# web3 and prometheus-client are mocked at import time and NOT required here.
# Use requirements.txt only when running the full application stack.
flask==3.0.0
flask-cors==4.0.0
pytest==7.4.0
pytest-mock==3.12.0
